
import loding from '../loding.gif'

const Spinner=()=>{
  
    return (
      <div className="text-center">
        <img src={loding} alt="I am loding" />
      </div>
    )
  }

export default Spinner
